// Создаем переменную
var year = 2019;

// Обращаемся к переменной
console.log(year);


// Создаем переменную с помощью ключевого слова let
let person;

console.log('Человек человеку', person);

person = 'друг';

console.log('Человек человеку', person);


// Создаем переменную с помощью ключевого слова const
const phrase = 'Значение этой переменной поменять нельзя';

phrase = 'А может все таки можно?';